export const zombiesReducer = (state, action = {}) => {
  switch (action.type) {
		// case "account.updated":
		// 	return {
		// 		...state,
		// 		account: {
		// 			...state.account,
		// 			...action.payload,
		// 		}
		// 	}
		default:
			return state
  }
}
